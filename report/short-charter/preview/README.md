# Preview policy

`page-*.png` files are direct 200-dpi `pdftoppm` renders of the compiled A4 sample PDF. Their intrinsic size is approximately 1654 × 2339 px. They must not be cropped, resized, composited, sharpened, annotated, or redrawn. README files reference them without HTML width/height overrides.
