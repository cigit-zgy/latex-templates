# Preview policy

`page-*.png` files are direct 72-dpi `pdftoppm` renders of the compiled A4 sample PDF. They must not be cropped, resized, composited, sharpened, annotated, or redrawn. README files reference them without HTML width/height overrides.
