# Short Charter Report

## Overview

Compact XeLaTeX `article` template for scientific and technical reports of approximately 8–12 pages. The document starts with the title page followed directly by report sections.

## Files

- `main.tex` — document entry point.
- `config/` — metadata and colour configuration.
- `style/` — typography, layout, and component definitions.
- `sections/` — report content.
- `scripts/` — build and font setup scripts.
- `preview/` — rendered sample pages.

## Typography

This template follows [`../../STYLE_SPEC.md`](../../STYLE_SPEC.md).

- Body: XCharter.
- Mathematics: XCharter-Math.
- Structural headings: Latin Modern Sans Bold.
- Chinese: LXGW WenKai Screen 1.522.
- Code: Maple Mono 5.3.0.

## Build

```bash
./scripts/build.sh
```

## Preview

| Title page | Hierarchy specimen |
| --- | --- |
| ![Short report title page](preview/title.png?rev=28f1192173fe) | ![Short report hierarchy specimen](preview/hierarchy.png?rev=867b222a93ff) |

## Constraints

- No table of contents is generated by default.
- `section`: 16/20 pt Bold `BrickRed`.
- `subsection`: 13/16 pt Bold `RoyalBlue`.
- `subsubsection`: 11/14 pt Bold `ForestGreen`.
- Manuscript tables use `AcademicTable` and occupy the full `\linewidth`.
- Preview PNGs are direct 150-dpi PDF renders.
