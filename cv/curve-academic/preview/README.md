# Preview policy

`page-01.png` is a direct 72-dpi `pdftoppm` render of the compiled A4 CV. The build refreshes the public GitHub avatar before compilation. The preview must not be cropped, resized, composited, sharpened, annotated, or redrawn, and README files must not set HTML width/height overrides.
