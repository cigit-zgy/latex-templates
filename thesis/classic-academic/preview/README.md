# Rendered preview

`preview.svg` is a repository-friendly visual preview of the template's compiled layout: title page, chapter opening, and representative scientific content.

The full PDF is generated during local validation rather than committed, keeping the template repository compact.
